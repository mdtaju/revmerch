import ShowRoom from "@/components/showroom/ShowRoom";

const ShowRoomPage = () => {
      return (
            <main>
                  <ShowRoom />
            </main>
      );
};

export default ShowRoomPage;