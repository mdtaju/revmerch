import Register from "@/components/register/register";

const RegisterPage = () => {
      return (
            <main>
                  <Register />
            </main>
      );
};

export default RegisterPage;